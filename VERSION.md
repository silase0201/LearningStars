# v2.0.0

目前版本：圖片只負責主視覺與選項插圖，問題及選項文字改由 HTML 表現。

- 24 張選項插圖不包含 ABCD、標題、說明文字、外框及色帶。
- 選項插圖保持長寬比例，以 `object-fit: cover` 填滿 `<picture>`。
- ABCD、問題卡、框線、背景、色帶、進度及選中效果由 CSS 表現。
- 所有 `font-size` 均使用 `rem`，手機與桌機由媒體查詢套用對應字級。
- 問題與選項文字集中在 `js/app.js`，可直接修改。
- 包含 PNG、WebP、切圖腳本與素材總覽。
- `body` 使用小型循環動畫 PNG 星空背景；減少動態模式改用靜態 PNG。
- 六題場景改為透明前景、獨立星體與 HTML 關卡文字的分層結構。
- 星體載入時會避開文字並防止互相擠壓，隨機套用景深、尺寸及緩慢漂移。
- 不再使用的舊素材已分類封存至 `source-images`，不建立新的版本資料夾。
- 六題 `.scene` 已重新繪製為 36 個透明獨立元件，不再載入單張合成前景。
- 每個場景元件可依桌機與手機分別設定位置、寬度及圖層。
- q04 星球、q05 書本與 q06 學習星改為固定的題意元件；裝飾星體仍維持隨機景深。
- 第一版場景素材已封存至 `source-images/legacy-assets/redrawn-scene-first-pass`。

入口：`index.html`
