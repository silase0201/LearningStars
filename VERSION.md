# v2.0.0

目前版本：圖片只負責主視覺與選項插圖，問題及選項文字改由 HTML 表現。

- 24 張選項插圖不包含 ABCD、標題、說明文字、外框及色帶。
- 選項插圖保持長寬比例，以 `object-fit: cover` 填滿 `<picture>`。
- ABCD、問題卡、框線、背景、色帶、進度及選中效果由 CSS 表現。
- 所有 `font-size` 均使用 `rem`，手機與桌機由媒體查詢套用對應字級。
- 問題與選項文字集中在 `js/app.js`，可直接修改。
- 包含 PNG、WebP、切圖腳本與素材總覽。

入口：`index.html`
